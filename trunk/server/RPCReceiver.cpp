#include "RPCReceiver.h"
#include "ClientEventHandler.h"
#include "RPC.h"

void RPCReceiver::handle(const RPC& rpc) {

    // switch (type) {
    // case RPC_EVENT:
    //     handle_event(tunnel, rpc);
    //     break;
    // case RPC_METHOD:
    //     break;
    // }
}

void RPCReceiver::handle_event(const RPC& rpc) {
    //    Event e;;
    // e.unserialize(stream);
    // _client_event_dispatcher->dispatch_event(e);
}
